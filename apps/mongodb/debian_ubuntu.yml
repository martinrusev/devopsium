- name: Add Mongo repository key.
  command: apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv 7F0CEB10

- name: Add repository - Debian.
  template: src=debian.repo dest=/etc/apt/sources.list.d/edgium.mongodb.list
  when: ansible_distribution == 'Debian'

- name: Add repository - Ubuntu.
  template: src=ubuntu.repo dest=/etc/apt/sources.list.d/edgium.mongodb.list
  when: ansible_distribution == 'Ubuntu'

- name: Update.
  sudo: true 
  command: apt-get update

- name: Install MongoDB.
  sudo: true 
  command: apt-get install mongodb-org

- name: Start MongoDB.
  sudo: true 
  command: service mongodb start