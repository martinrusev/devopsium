- name: Add repository.
  template: src=rpm_repo.j2 dest=/etc/yum.repos.d/falcon.mongodb.repo

- name: Install MongoDB.
  command: yum install mongodb-org

- name: Start MongoDB.
  command: /etc/init.d/ mongodb start