#! /bin/bash
PLUGINS_REPO=/usr/local/colonizer
REMOTE_REPO=https://github.com/martinrusev/colonizer.git

set -e

case "$1" in
    configure)

        rm -rf $PLUGINS_REPO
        git clone $REMOTE_REPO $PLUGINS_REPO
        
        set +e

    ;;
    abort-upgrade|abort-remove|abort-deconfigure)
    ;;

    *)
    ;;
esac

#DEBHELPER#

exit 0
